import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        zIndex: 1,
        padding: 20,
        left: 0,
        right: 0,
        position: 'absolute',
        justifyContent: 'space-between',
        flexDirection: 'row',
    },
    button: {
        padding: 10,
    },
});
