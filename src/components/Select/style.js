import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        borderWidth: 1,
        borderColor: '#d1cfd1',
        marginRight: 5,
    },
    button: {
        padding: 10,
    },
});
