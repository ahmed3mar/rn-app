import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        borderWidth: 1,
        borderColor: '#d1cfd1',
    },
    input: {
        padding: 7,
        color: '#303030',
    },
});
