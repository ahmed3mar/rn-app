import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    tabBar: {
        flex: 1,
        padding: 10,
        alignItems: 'center',
        borderTopWidth: 2,
        borderTopColor: '#EFEFEF',
    },
    tabActive: {
        borderTopColor: '#6073E8',
    },
    label: {
        fontWeight: 'bold',
        fontSize: 16,
    },
});
